# A2A Mapping (stub)

This document will describe how ADP manifests map to the A2A AgentCard format. The `interop.a2a` block in ADP is reserved for either embedding an AgentCard or referencing one by path/URL. A future revision will provide a precise field-by-field mapping and validation guidance.
